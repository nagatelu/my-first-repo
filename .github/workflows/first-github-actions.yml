name: Run all commands
on: [push]
jobs:
  run-shell-command:
    runs-on: ubuntu-latest
    name: echo a string
    steps:
      - name: echo a string
        run: echo "hello this is my first github actions file"
